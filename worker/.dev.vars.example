# คัดลอกไฟล์นี้เป็น .dev.vars (ห้าม commit .dev.vars จริง) สำหรับรัน `npx wrangler dev` ในเครื่อง
# secret จริงบน production ให้ตั้งด้วย `npx wrangler secret put <NAME>` (ดู README "เริ่มงาน (Setup)")

LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=
