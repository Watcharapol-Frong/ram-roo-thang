{
  "name": "ram-roo-thang-bot-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --secrets-file .secrets.env",
    "deploy:no-secrets": "wrangler deploy",
    "secrets:push": "wrangler secret bulk .secrets.env",
    "secrets:check": "wrangler secret list",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
